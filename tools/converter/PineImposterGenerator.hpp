#pragma once

#include "PineTreePackConverter.hpp"

// Generates registered side and cumulative top-down material captures.
bool GeneratePineImposters(
    ImportedPack* pack,
    std::string* error);
