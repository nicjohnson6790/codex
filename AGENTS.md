# Agent Working Notes

This file is the durable handoff point for coding agents working in this repository. Read it at the start of a session and update it when work changes an enduring project fact, workflow, constraint, or unfinished thread.

Keep notes concise and current. Do not append command transcripts or routine progress logs. Remove or rewrite obsolete notes when their underlying work is completed.

## Project orientation

- Codex is a Windows C++20 terrain sandbox using SDL3 GPU, Dear ImGui, CMake, Ninja, and GLSL compiled to SPIR-V.
- The root [README.md](README.md) is intentionally an onboarding document. Detailed rendering design belongs in [docs/codex_rendering_architecture.md](docs/codex_rendering_architecture.md).
- Canonical builds are `tools\build.cmd Debug`, `tools\build.cmd Release`, and `tools\build.cmd Assets [Debug|Release]`; the Assets configuration defaults to Release.
- `tools\build.cmd Assets [Debug|Release]` builds the converter only. Every asset-pack conversion is an explicit `build\Assets\<Config>\converter\converter.exe <pack>` operation.
- Canonical build directories are `build/Debug`, `build/Release`, `build/Assets/Debug`, and `build/Assets/Release`. Avoid creating alternate build-directory names for routine validation.
- Keep target runtime outputs isolated: the app and its staged resources belong in `build/<Config>/app`, tests in `build/<Config>/tests`, and the asset converter in `build/Assets/<Config>/converter`. Do not stage target outputs directly into a canonical build root.
- Generate local runtime metadata such as `steam_appid.txt` directly in `build/<Config>/app`; do not create an intermediate copy in the canonical build root.
- The build/configure scripts detect CMake caches that reference a removed MSVC compiler and automatically reconfigure with `cmake --fresh`.
- Windows `.cmd`/`.bat` scripts must retain CRLF working-tree line endings (enforced by `.gitattributes`); LF-only scripts can fail with missing batch subroutine labels before compilation.

## Durable constraints

- The app and converter pin SDL 3.4.16. Upstream fixes 3D subresource barriers; the app applies `cmake/PatchSDLVulkanFullTexture.cmake` to use `VK_REMAINING_ARRAY_LAYERS` for 3D full-texture barriers too. Array/cubemap counts stay unchanged. Native type/count metadata is valid before defrag container assignment; no local texture field is needed. The patch is idempotent and rejects unexpected/ambiguous source; review/remove on SDL upgrades. Validation and device features remain unchanged. The app links with an explicit SDL DLL dependency so DLL-only fixes trigger runtime staging. See `docs/sdl_upgrade_handoff.md`.

- The entire `assets` directory is ignored by Git except for shared format/reader source code under `src/assets`.
- Runtime asset packs are generated into `assets/runtime` and staged into each app build. Do not commit generated packs or externally licensed source assets.
- Steamworks support is optional. The default SDK location is `../deps/steamworks_sdk_164/sdk`; local runs can use `--disable-steam`.
- The ETOPO converter is an explicit offline operation, not part of the normal asset build. Its format is version 4 and its fixed Airocean projection remains version 3.
- ETOPO is the initial runtime terrain source. `WorldGridQuadtreeHeightmapManager` owns tiled source residency, affine source placements, retained final-to-source references, and composed-final scheduling; `QuadtreeMeshRenderer` retains all GPU resource ownership. User-created heightmap layers remain future work.
- Terrain heightmaps, canonical foliage, canopy, and nearby decoded foliage use the shared `FixedAssetCache` / `GenerationQueue` residency protocol in `src/AssetResidency.hpp`; preserve transactional queue-before-cache admission, ready-only request results, manager-owned shared fences, and handle-based stale-result retirement. Nearby residency is owned by `WorldGridNearbyFoliageManager`, not its renderer.
- `FixedAssetCache` lookup storage is `bucketCount * entriesPerBucket`, with a cache-capacity overflow list consulted only for buckets marked overflowed. Do not replace this with bounded linear probing plus unconditional full-cache fallback scans.
- Node-associated final-heightmap, canonical foliage-page, canopy-cell, and nearby-foliage cache hints live in `WorldGridQuadtree::m_residencyHints`, a fixed-capacity array parallel to `m_nodes`. Hints survive frames until node-slot reuse; use this storage instead of temporary hint-preservation bookkeeping. Multi-asset entries follow the semantic caches' ordering, and nearby topology shares canonical foliage entry zero.
- Hints are disposable lookup acceleration, never semantic identity. Requests directly replace stored hints with their return values, including the common `kUnavailableCacheIndex` from `AssetResidency.hpp`; caches validate hinted slots against semantic IDs. Cache clears need no tree-wide hint reset. Parents and children own independent hints and access each other's state through tree links; prospective unallocated nodes still use ordinary requests. Source-heightmap references/hints remain a separate manager-owned mechanism.
- Read-only residency lookups use `hint = manager.isResident(id, hint)`: ready slot or unavailable, without admission, generation, or age touching. Slot-based `build...` data accessors validate semantic identity and payload readiness without hash lookup. Quadtree queries, including neighbors/debug/water, reuse node hints; collision tiles retain their own upstream hints.
- `CollisionManager` uses a 16-slot `FixedAssetCache` for tile identity, ground readiness, and eviction; tree readiness remains independent payload state. It uses `uint64_t` elapsed-frame ages to preserve timestamp eviction ordering beyond 255 frames. Other caches keep the default saturating `uint8_t` age. Collision payload and upstream hints reset on slot reassignment; copied CPU data does not depend on continued upstream residency.
- Preserve unrelated working-tree changes and use `apply_patch` for source/text edits.

## Session handoff notes

- User accepted the completed foliage result, including corrected normals and continuous imposter tilt. Build-folder logs/temporary inspection files and the unused Assets Debug build were removed; runtime Debug/Release, Assets Release and saved horizon diagnostics remain. Validation is recorded in rendering architecture section 5.4. Detailed GPU timings remain unmeasured.
- Foliage schema 2 stores full signed tree-local XYZ normals in linear BC3 for both imposters and canopy. Preserve signed Z through filtering and apply only instance rotation at runtime. The 64-byte extension is referenced by AssetRecord offset 20; other pack schemas stay v4. Old pine packs must be regenerated; local packs were regenerated and validated. Capture registration uses imported Y=0 ground without independent mesh recentering.
- Imposters tilt continuously over 0–75 degrees and blend two yaw views at each adjacent pitch, superseding the ticket's nearest-pitch restriction. Separate UVs register each pitch's serialized bounds on a quad enclosing both; outside-frame samples are transparent. Color/depth share foliage_imposter_blend.glsl coverage; normals use the same alpha weights. Existing 0/25/50/75 captures suffice.
- Canopy uses the user-requested fixed four-candidate lattice approximation, direct halo slot/seed lookup and compute occupancy bits. Keep direct SSBO indexing, early fade/seed rejection, base-level alpha cutout, gradient-filtered RGB/normals, and half-open sky receiver domains. Larger crowns can overlap from candidates outside this approximation.
- All foliage paths borrow terrain sky regions and prepared clouds through FoliageLighting without probe scheduling changes. Receiver hints refresh before upload. Nearby cutoffs are 5/25 m with the original 100–105 m fade. Canopy draw metadata is 864 bytes; tree-class stride is 64 bytes. Debug/Release/Assets Release builds, seven tests per runtime configuration, affected SPIR-V validation, production pack regeneration, GPU startup/traversal and framing/normal checks passed.

- User accepted automatic exposure limits −8/+6 EV. The terrain sunlight multiplier was rejected and removed. Fallback commit `0c66927` (terrain night lighting/direct cloud shadows and accepted exposure limits) is pushed to GitHub main. Terrain now replaces the ambient floor with sky/cloud irradiance probes; see architecture section 19. Do not add a direct-sun calibration multiplier or apply camera exposure to probe data.

- SkyIlluminationManager receives only successfully emitted frustum-visible terrain leaves, maintains a 1024-slot FixedAssetCache and schedules N=4 tile refreshes/frame (Controls / Sky illumination, 1–32). Each tile has 4×4 ground probes and each probe integrates 16×16 equal-solid-angle upper-hemisphere directions, with the sun disk excluded. SkyIlluminationRenderer owns a persistent 9-RGB-SH-coefficient field (Lambert convolution baked in), frame-region buffer and compute pass; other renderers can borrow the two read-only buffers and obtain a frame-local region hint by leaf ID. Foliage now borrows these buffers; water is not a consumer yet. Sample heights are bilinear final heightmaps +2 m. Monotonic height-generation stamps force fresh initialization after regeneration/reassignment. Retained ancestors are fallback data only, never hidden update work. The command queue orders compute refreshes before terrain reads; no CPU readback, asynchronous worker or new generation queue is involved.
- Ground probe axes use cell-centered fractions 0.125/0.375/0.625/0.875 (half-pitch margins); shader interpolation uses fraction × 4 − 0.5. Sky probe updates blend over elapsed time (0.25 s); missing tiles start one refresh cycle old so continuous flight cannot starve retained lighting. All visible tiles/ancestors are protected before cache admission. Shared smooth spatial support and conservative candidate lists give equal-position sampling the same contributors across body/bridge and mixed-LOD boundaries. Probe coefficients remain world-space directional data across render-origin changes. New/uncovered regions temporarily use a retained ancestor or nearest available sky grid. Debug/Release builds, all six tests in each, all 80 SPIR-V modules, Release startup and the 720-frame Debug traversal passed (12 stages, 24 macro revisions, 1,750 tile probe updates; no logged validation errors). Visual quality, transition lag, coarse-height approximation and GPU timing at N=4 remain for user review.

- Terrain direct sunlight retains the named 0–+1 degree smooth horizon transition and one solar-visibility × Beer-transmission factor for direct/specular and masked caustics, never sky irradiance. Direct terrain irradiance uses shared surfaceSunIrradiance: the existing solar RGB/intensity × atmosphere/cloud source scale × environment scale, with air transmission at ground altitude. This corrects the unscaled direct source that let cloud probes overwhelm sunlight; no independent terrain multiplier exists. CloudRenderer prepares phases once; terrain uploads a 240-byte shadow/lighting block at fragment slot 1 (field 0, params 128, optics 144, atmosphere 224). Both body/bridge paths bind nine samplers (cloud coverage/noise 7/8) plus two sky-illumination storage buffers (shader bindings 9/10). Terrain uniforms stay 336 bytes; only elevation-vector X at offset 96 is used. Body/bridge varying 3 identifies the same frame-local illumination receiver region. The finite footprint/slab bounds direct shadows; Clouds UI shadow samples default to 8, clamped 1–32. See architecture sections 17 and 19.

- Temporal terrain/water culling was removed after user review found missing bridges, alternating-frame flicker and marginal benefit. Both renderers retain separate body/bridge pipelines but share one parent descriptor SSBO per renderer. The heightmap manager accepts one terrain parent with resolved edges; the water manager accepts one parent with complete masks. No attach-to-last-parent calls remain. ParentMeshDescriptors appends 4-byte parent/edge references partitioned normal then coarse; CPU upload prepares all three indirect counts. Terrain records are 80 bytes, water 48; C++ offsets and GLSL capacities must agree. No history textures, visibility compute or compacted descriptor outputs remain. See rendering architecture section 18.

- Shared parent descriptor validation passed Debug/Release builds, all six tests in each, SPIR-V validation and compiled stride/offset inspection, and the 720-frame Debug GPU traversal (12 stages, 24 macro revisions; no reported validation errors). User flight review accepted the result. RenderDoc sample 6 confirms 92 terrain parents with all 368 bridges and no visibility dispatch/depth-history copy; per-event timing anomalies still prevent reliable whole-frame comparisons.

- Directional macro cloud detail uses one extra red-channel LOD-0 sample of the existing volume, multiplied before the coverage offset/clamp. `CloudRenderer` owns bounded double travel/evolution phases and prepares the shared 304-byte sampling state every upload, independently of coverage revision. The float-rounded 2D transform feeds `PeriodicWorldPhase`; +angle rotates +X toward +Z, transverse stretch elongates perpendicular to d, and subtracting travel moves visible features along +d. Evolution is texture cycles/hour on the altitude-independent slice starting at 0.5. Zero speeds freeze phases; settings edits, origin/cache changes and resize preserve them. Shared `cloud_shape.glsl` applies the specified exponential/terminal-smoothstep top factor before erosion, preserving the bottom. New defaults and visual review scenarios are in rendering architecture section 17; GPU cost of the extra density lookup (including sun marches) remains unmeasured.

- The viewport HDR conversion is implemented: SDLRenderer owns RGBA16_FLOAT linear Rec.709 scene color plus ordinary UNORM display color and shared depth. DisplayTransformRenderer owns GPU histogram/adaptation (automatic by default, manual 1 preserved), applies the sole camera exposure, exponential shoulder and exact sRGB encoding, then triangle/line/text overlays render with scene depth. Metering uses three queue-ordered non-cycling compute passes and persistent value/validity state; preserve first-valid initialization, manual seeding and resize continuity. Current implementation, numerical diagnosis, sampling bounds and pending GPU timing review (preceding visual workgroup accepted): docs/atmosphere_exposure_handoff.md. Prior fixed-horizon evidence: docs/hdr_conversion_handoff.md.

- Water reflection and non-TIR underside transmission share `cloud_integration.glsl` with the fullscreen cloud pass. `CloudRenderer::upload` prepares field/phases and common optics once in the early copy pass; water borrows its two texture/sampler bindings and 304-byte sampling state (fragment uniform slot 1). Both water paths bind eight samplers. Water budgets round primary counts times the Clouds UI multiplier (default 0.5: 24/3; one-third: 16/2). Keep cloud ambient distinct, water ambientSky clear-only, and scene-depth clipping fullscreen-only. Clear-sky audit found only the missing above-atmosphere downward distance extension, now corrected; no sea-level color retuning. Visual/performance acceptance remains pending user review.

- Clouds use a synchronous 32-slot `CloudManager` cache of 17x17 coverage samples per 2^19 cell. Its desired 5x5 set comes from the terrain's actual active center. Positive-edge samples canonicalize before hashing; protect all retained desired slots before new admissions. `CloudRenderer` writes the complete 81x81 coverage image straight into a reusable padded transfer buffer, owns the one GPU-generated periodic RGBA8 32x32x64 volume, and composites after the existing sky pass. No generation queue, CPU volume, readback or offline pack is involved. Terrain shadows borrow these same resources.
- Water/cloud integration validation passed Debug/Release builds, all six tests in both configurations, SPIR-V validation/layout inspection, a Release startup frame, and the 720-frame Debug cloud traversal (12 stages, 24 revisions; no validation errors). These checks do not establish underwater visual quality or performance; review the scenarios recorded in rendering architecture section 6.
- Shared `cloud_density.glsl` takes origin-relative positions plus camera-independent field uniforms; preserve identical bounded base/detail phases for future shadow sampling. Noise scales are cycles/km in settings, cycles/m in uniforms. Integrate linear radiance with analytic step transmission before display mapping. A single slab-length sun march feeds every dual-HG scattering octave and the view-dependent powder term; future terrain shadows must use pure Beer transmission only. `cloud_tests` covers borders/cache churn, seed changes, universe phases, actual shader noise periodicity, slab cases, and optical limits. `--verify-clouds --quit-after-frames 720 --disable-steam` runs the GPU traversal sweep.
- Cloud GPU traversal passed all 12 stages with 24 macro revisions and no validation errors. The user visually tuned and approved new defaults: thickness 6500 m, coverage 0.15, density 3.5, base/detail frequencies 0.063/0.052 cycles/km (corrected after user tuning), base/detail strengths 0.08/0.28, and erosion 0.7. These supersede the ticket's initial reference settings; other defaults remain unchanged.

- The offline ETOPO heightmap converter and stale-MSVC-cache recovery are committed on local `main`.
- The rendering architecture document now also covers the offline asset boundary and multiplayer render-emission boundary.
- Shared asset-cache/generation-queue infrastructure and migrations are implemented locally; focused coverage is in `tests/AssetResidencyTests.cpp` and built by default as `asset_residency_tests`.
- Runtime heightmaps now compose cached 256x256 ETOPO source tiles through contribution descriptors; repeated placements share source residency and remain additive.
- Terrain materials, water FFT sampling, foam detail, and terrain caustics use CPU-computed periodic phases from `Position` via `src/PeriodicWorldPhase.hpp`; keep repeating shader coordinates render-origin-relative rather than reconstructing absolute X/Z floats.
- The offline `japan-dem10` converter path creates four fixed rotated 10 m ETOPO-relative delta mosaics from Source Cooperative's manifest-backed EPSG:6668 DEM10 files. The runtime registers available mosaics as additive sources only for final-heightmap pitches at or below 32 m; coarser terrain remains ETOPO-only.
- Terrain bridge instances carry independently resolved inner, outer, and two corner final-heightmap slices. `WorldGridQuadtree` owns neighbor, half-edge, and corner resolution; preserve the existing bridge mesh topology and renderer-side GPU ownership.
- Bridge corner ownership also checks the actual diagonal leaf for a resident 2:1 coarser tile, including across base roots; cached subdivided parents cannot own corners. Regression coverage is in `terrain_corner_neighbor_tests`.
- Terrain bridge edge records are 16 bytes within each 80-byte parent descriptor. Owner-relative corner selectors occupy metadata bits 0–2 and 3–5 (SW,S,SE,W,E,NW,N,NE); the shader derives sample coordinates. Scale/edge/half retain bits 16–23/24–25/26. Keep the C++ and GLSL layouts synchronized.
- Corner selectors come directly from corner orientation, neighbor edge and half, or the opposite diagonal corner. Do not reconstruct positions, subtract world coordinates, or divide by pitch to select these eight samples.
- The user confirmed the diagonal corner seam is fixed after replacing position-derived corner samples with direct edge/half/diagonal selector lookup.
- Japan DEM10 conversion retains decoded ETOPO tiles and uses a bounded eight-raster DEM10 working set; preserve these caches to avoid severe boundary-thrashing regressions.
- Japan DEM10 bilinear sampling resolves each lattice point through the raster catalog so footprints crossing Source Cooperative GeoTIFF boundaries do not become zero-delta seams. Preserve the cross-raster self-test in `JapanDem10Converter.cpp`.
- Runtime heightmap format v4 stores per-tile float scale/bias and signed 16-bit codes; INT16_MIN is invalid and INT16_MIN+1 is exact additive zero. ETOPO stays float until final tile quantization; regenerate ETOPO before Japan DEM10. Runtime source uploads retain normalized 16-bit codes; GPU sampling applies per-tile scale/bias before interpolation, independently of layer yScale. Shared converter quantization and round-trip tests live in `tools/converter/HeightmapQuantization.hpp` and run in `heightmap_dataset_tests` and both converter self-tests.
- Japan DEM10 generation includes a 2 km fading ocean collar around source coverage that cancels positive ETOPO elevation without replacing existing bathymetry.
- CPU final heightmaps use a separate 16-entry FixedAssetCache and one fixed sample allocation, with eight GenerationQueue readback jobs copied directly from renderer transfer buffers. Final invalidation discards dependent CPU work; queued downloads retain job/destination ownership through renderer retirement.
- Source decode uses one asynchronous pipeline with exactly two shared 128 KiB staging buffers, reconstructing directly into mapped 16-bit upload storage. Packed GPU source words hold two signed codes; normalization maps invalid/exact-zero to 0 and swaps valid code 0 to -32767 so tile bias is preserved. The shader reverses that swap, decodes scale/bias per sample, then interpolates and applies yScale. Final-to-source references use 512 fixed regions of 64 entries.
- Source requests queue bounded metadata in the 64 source slots. A worker drains batches back-to-back through the shared staging pair into separately mapped renderer transfer buffers (64 × 128 KiB total). Keep SDL mapping/unmapping on the render thread, pin loading slots through upload completion, and never gate each tile's decode on a frame boundary. GPU stress validation requires multi-tile decode/upload batches.
- Run `--stress-heightmap-pipeline --quit-after-frames 3600 --disable-steam` for GPU traversal/readback churn and pending invalidation checks; the run fails if required transitions were not observed.
- Offline heightmap conversion uses `LZ4_compress_HC` at `LZ4HC_CLEVEL_MAX` for every tile, preserving the <=128 KiB bound and ordinary runtime LZ4 format. `converter.exe heightmap-repack <assetbin>` still losslessly migrates oversized existing v3 blobs; the 37 local ETOPO/DEM10 CE/NE tiles were migrated and `.assetbin.before-bounded` backups retain their original indices.
- ETOPO and Japan DEM10 use hardware-concurrency-sized, long-lived async workers through `HeightmapTileWorkers.hpp`. Workers reuse tile/filter/round-trip buffers; Japan workers own independent eight-raster FIFO caches (reusing evicted allocations), TIFF read buffers, retained ETOPO caches, and file streams across all four mosaics. ETOPO's immutable float raster is shared. Only append/index publication and progress aggregation lock; payloads follow completion order, records occupy the full 256x256 spatial table. Runtime and converter source lookup use `(y + 128) * 256 + (x + 128)` after range checks, with `compressedSize == 0` marking absent slots. `heightmap_dataset_tests` covers unordered concurrent writes, HC policy, context reuse, runtime reads, and worker errors; `--compare <first.assetbin> <second.assetbin>` verifies logical equality across conversions.
- Format v4 assetbins contain a 232-byte header and all 65,536 32-byte records (2,097,384 bytes total), including zero-filled absent slots. Header tileCount counts present tiles. Shared `RuntimeHeightmapIndex.hpp` validates layout, coordinates, presence/count, and metadata. `heightmap-reindex` losslessly migrates v3 indices with `.before-spatial` backups and leaves heightbin untouched; all five local runtime packs are migrated. Tests cover the full coordinate domain, edge/range rejection, empty and malformed tables, and migration equivalence.
- SkyboxRenderer now composites background, RGB transmission, and additive single scattering with three fullscreen draws; the old final-color atmosphere LUT and LUT-only controls are removed. Shared `shaders/atmosphere.glsl` uses analytic clamped exponential columns and eight Gauss-Legendre samples in inverted column density; water/nearby foliage use it for environment lighting. `atmosphere_math_tests` compiles the shader math on CPU and compares it to dense integration.
- Sky/environment consumers share an sRGB-decoded cubemap with separate space scale 0.02 (the old 0.0001 rounded even white texels to black at manual exposure 1). Directional sky/water rays add a 0.266-degree disk with independent solar-RGB calibration 100 before air transmission; linearSkyRadiance ambient remains disk-free. The user accepted these source defaults with the preceding HDR/exposure workgroup. evaluateAtmosphere applies atmosphere/cloud source scale 4.8 and environment scale 12 once to all scattering, never transmission. Clouds retain 4.8 in incident sunlight and apply 12 after the complete local lighting expression (including sqrt ambient), then return accumulated premultiplied radiance directly. Water medium sourceScale remains 4.8 independently. No per-medium camera exposure remains.
- Fullscreen clouds intersect the prepared 5x5 footprint and slab, clipping only to foreground depth. That cloud set encloses the terrain's actual 3x3 roots. Distance-aware quadratic sampling uses 48 minimum samples and a default 512 total ceiling (up to 1024); no radial cutoff remains. Water retains the separate 180 km limit, uniform counts and existing multiplier. Shared footprint helpers, integer-origin subtraction and field phases must remain aligned. The preceding footprint visual review is accepted; per-pass GPU cost remains unmeasured as recorded in docs/atmosphere_exposure_handoff.md.
- Underwater classification stays GPU-side using water-owned FFT maps, shared wave/damping helpers, periodic camera phases, and existing emitted terrain metadata. Air/water modes are exclusive. Water RGB coefficients live in AppConfig and the sky settings UI. Mixed-medium camera paths remain deferred.
- The shared water fragment shader uses fixed IOR 1.333 and derived F0, with water-to-air Snell refraction, exact underside Fresnel, and TIR. Underside sky transmission uses sampleSkyRadiance; reflection uses the infinite surface-origin limit of the existing isotropic waterMediumRadiance helper shared with the sky compositor. Keep underside alpha one and leave camera-side attenuation to the compositor. Existing medium settings are passed through the matching C++/GLSL water uniform layouts; no independent IOR/F0 control exists. atmosphere_math_tests compiles both optical helpers and checks critical-angle behavior and the infinite-medium limit.
- Two-sided water validation: Debug/Release builds and all five tests pass, as do SPIR-V validation and a temporary 960-frame GPU camera sweep (above water, -0.25/-3/-20 m, critical/grazing angles, repeated crossings; regular and bridge instances emitted in every stage). The sweep code was removed. Visual acceptance remains pending because desktop app approval timed out; next verify Snell-window appearance, foam/shoreline/topside preservation, and bridge/crossing artifacts interactively.
- Add concrete unfinished work here only when it must survive into another session; include the relevant file or subsystem and the next useful action.
- Foam orbital motion uses the final rendered `displacement.xz` from both water vertex shaders (varying location 6), subtracted once at full strength to form `foamXZ`. The user superseded the ticket's fixed-history requirement: all foam sampling, including coverage/history and its noise warp, shares `foamXZ` with breakup/SDF detail. Preserve the existing shared noise-sample reuse, periodic phases, and simulation history generation/decay; do not add second depth damping or alter non-foam sampling. The user visually confirmed the combined foam motion looks good; Debug/Release builds, SPIR-V validation, periodic-coordinate tests, and the 120-frame GPU check passed.
